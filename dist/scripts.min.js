let pokemonRepository = (function() {
  let e = [],
    t = document.createElement('img');
  function n() {
    t.classList.add('message-show');
  }
  function i() {
    t.classList.add('message-hide');
  }
  function l(t) {
    return e.push(t);
  }
  function o(e) {
    a(e).then(() => {
      let t = e.name,
        n = e.height,
        i = e.imageUrl,
        l = document.querySelector('.modal-container');
      function o() {
        l.classList.remove('is-visible');
      }
      !(function(e, t, n) {
        l.innerHTML = '';
        let i = document.createElement('div');
        i.classList.add('modal-dialog', 'modal-dialog-centered'),
          i.setAttribute('role', 'document');
        let a = document.createElement('div');
        a.classList.add('modal-content', 'modal');
        let d = document.createElement('div');
        d.classList.add('modal-header');
        let s = document.createElement('h5');
        (s.innerText = e),
          s.classList.add('modal-title'),
          s.setAttribute('id', 'exampleModalCenterTitle'),
          d.appendChild(s);
        let c = document.createElement('button');
        c.classList.add('close'),
          (c.innerText = 'close'),
          c.setAttribute('data-dismiss', 'modal'),
          c.setAttribute('aria-label', 'close'),
          c.addEventListener('click', () => {
            o();
          }),
          d.appendChild(c);
        let r = document.createElement('div');
        r.classList.add('modal-body');
        let m = document.createElement('p');
        (m.innerText = 'Height : ' + t), r.appendChild(m);
        let u = document.createElement('img');
        (u.src = n),
          u.classList.add('pokemon-image'),
          r.appendChild(u),
          a.appendChild(d),
          a.appendChild(r),
          i.appendChild(a),
          l.appendChild(i),
          l.classList.add('is-visible');
      })(t, n, i),
        window.addEventListener('keydown', e => {
          'Escape' === e.key && l.classList.contains('is-visible') && o();
        }),
        l.addEventListener('click', e => {
          e.target === l && o();
        });
    });
  }
  function a(e) {
    n();
    let t = e.detailsUrl;
    return fetch(t)
      .then(function(e) {
        return e.json();
      })
      .then(function(t) {
        i(),
          (e.imageUrl = t.sprites.front_default),
          (e.height = t.height),
          (e.types = t.types);
      })
      .catch(function(e) {
        i(), console.error('Error when validating item ', e);
      });
  }
  return (
    (t.src = 'img/loading.jpeg'),
    t.setAttribute('width', '80px'),
    t.setAttribute('height', '80px'),
    document.querySelector('.pokemon-list').appendChild(t),
    {
      getAll: function() {
        return e;
      },
      add: l,
      addListItem: function(e) {
        let t = document.querySelector('.pokemon-list'),
          n = document.createElement('li');
        n.classList.add('list-item', 'list-group-item', 'col');
        let i = document.createElement('button');
        (i.innerText = e.name),
          i.classList.add(
            'list-button',
            'btn',
            'btn-success',
            'list-group-item-action'
          ),
          i.setAttribute('data-toggle', 'modal'),
          i.setAttribute('data-target', '#exampleModalCenter'),
          i.addEventListener('click', () => {
            o(e);
          }),
          t.appendChild(n),
          n.appendChild(i);
      },
      showDetails: o,
      loadList: function() {
        return (
          n(),
          fetch('https://pokeapi.co/api/v2/pokemon/?limit=150')
            .then(function(e) {
              return i(), e.json();
            })
            .then(function(e) {
              e.results.forEach(function(e) {
                l({ name: e.name, detailsUrl: e.url });
              });
            })
            .catch(function(e) {
              i(), console.error('Error when validating item', e);
            })
        );
      },
      loadDetails: a
    }
  );
})();
pokemonRepository.loadList().then(function() {
  pokemonRepository.getAll().forEach(function(e) {
    pokemonRepository.addListItem(e);
  });
});
